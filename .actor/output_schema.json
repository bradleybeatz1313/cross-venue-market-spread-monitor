{
    "actorOutputSchemaVersion": 1,
    "title": "Cross-Venue Spread Monitor Output",
    "properties": {
        "dataset": {
            "type": "string",
            "title": "Candidate spreads",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        },
        "summary": {
            "type": "string",
            "title": "Run summary",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
        }
    }
}
